# Copy to .env.local and add your key from https://aistudio.google.com
# Never commit .env.local. Never rename this to a VITE_ prefixed variable —
# Vite inlines those into the client bundle.
GEMINI_API_KEY=
ANTHROPIC_API_KEY=