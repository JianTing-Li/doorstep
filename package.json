{
  "name": "doorstep",
  "private": true,
  "type": "module",
  "description": "Deployment manifest for the Vercel project root. Each product under its own folder keeps its own package.json; this file exists so Vercel can install dependencies for the serverless functions in /api.",
  "dependencies": {
    "@google/genai": "^2.18.0"
  }
}
